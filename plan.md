# Main v0 prompt:

"Can you help me build a very simple and clean stationary shop for a small personal business, it should all be contained in a few pages, the main page is a single modern landing page style with 3 tiers where the first tier is the homepage landing with some simple marketing text, the second tier is a shop which has a few shop items in a columnar layout (ie 3 items), when you click in on a shop item it takes you to a product page where you have product details, and all you can do is input text into a "name" field, select the quantity and then hit "add to cart". On the main page the bottom tier should be an about/contact section. Implement navigation at the top of the page to make it easy to jump between different sections. Implement a cart system (via some kind of chip at the bottom right or top right of the screen) which is shared across the main page and the individual product pages, and when the user wants to finalize their order, show a confirmation dialog which requires that they enter their name, email, phone number and address, let them know shipping is including with the price."
